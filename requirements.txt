# Core
PyYAML>=6.0
requests>=2.28
croniter>=1.3
lark-oapi>=1.3

# Optional: Gemini API (for history compression fallback & briefing)
google-genai>=1.0

# Optional: image compression (isolated to subprocess)
Pillow>=9.0
